import React from 'react';
import { connect } from 'react-redux';
import './Favorites.css';

export const Favorites = ({ favoriteLaunches }) => {
  return (
    <div>
      <h1>Test</h1>
    </div>
  );
};

export default Favorites;
